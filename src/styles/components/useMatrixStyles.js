import { createUseStyles } from "react-jss";

export const useMatrixStyles = createUseStyles({
  root: {
    height: "100vh",
    width: "100vw",
    position: "absolute",
    top: 0,
    left: 0
  }
});
